import { PlaceholderScreen } from '@/features/shared/placeholder-screen';

export default function PaymentsRoute() {
  return (
    <PlaceholderScreen
      description="Filtros, resultados, totales globales y detalle de cada operación."
      next="El siguiente bloque incorporará filtros completos, tabla web y lista móvil usando fixtures contractuales."
      title="Pagos"
    />
  );
}
